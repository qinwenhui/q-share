## What / Why

What does this change do, and what problem does it solve? Reference the issue
if there is one (fixes #…).

## Test plan

- [ ] `cargo fmt --all -- --check`
- [ ] `cargo clippy --workspace --all-targets -- -D warnings`
- [ ] `cargo test --workspace`
- [ ] `cd web && npm run build` (if the frontend changed)
- [ ] Manual verification: how did you exercise the change?

## Screenshots / logs

Include them if the change is user-visible (GUI/TUI/web).
